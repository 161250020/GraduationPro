class Email:
    def __init__(self, title, from_email, to_email, doc, split):
        self.title = title
        self.from_email = from_email
        self.to_email = to_email
        self.doc = doc
        self.split = split
